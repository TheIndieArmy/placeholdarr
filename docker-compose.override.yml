services:
  # This file is intentionally for local machine overrides only.
  # Keep repo-default runtime config in docker-compose.yml.
  # The base compose pulls a published GHCR image for normal Docker users.
  # Use docker-compose.dev.yml when you explicitly want a local source build.
  # Leave this file for machine-specific port/network/path tweaks only.
  placeholdarr:
    # Example local-only override: bind web UI to localhost and custom port.
    # ports:
    #   - "127.0.0.1:18843:8000"

  placeholdarr_postgres:
    # Example local-only override: expose Postgres port for debugging.
    # ports:
    #   - "127.0.0.1:5432:5432"
